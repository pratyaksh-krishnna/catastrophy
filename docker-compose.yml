services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_PASSWORD: catastrophy
      POSTGRES_DB: catastrophy
    ports: ["127.0.0.1:55432:5432"]
    volumes:
      - ./docker/init-test-db.sql:/docker-entrypoint-initdb.d/010-test-db.sql:ro
